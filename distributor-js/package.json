{
  "name": "root",
  "private": true,
  "scripts": {
    "lerna": "lerna",
    "test": "yarn lerna run --parallel test",
    "clean": "yarn lerna exec -- rm -rf ./node_modules"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  },
  "devDependencies": {
    "lerna": "^3.19.0",
    "typescript": "^4.1.3"
  }
}
